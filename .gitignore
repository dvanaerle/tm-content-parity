node_modules/
# Generated data stays out of git, with four exceptions. The seed list is the
# input every later stage reads (ticket 51). The sitemap extract and its manifest
# are the production evidence the log's page counts can be checked against, and
# they are 289 KB against 181 MB of source (ticket 52). The status measurement is
# the answer to ticket 22, which exists because 451 phantom status values were
# recorded and nobody could check them (ticket 53).
data/*
!data/10-store-seeds.json
!data/sitemap-extract.json
!data/sitemap-manifest.json
!data/11-page-status.json
# The run log's index is committed — it is the one artefact of a run that cannot be
# rebuilt, because the run that saw a finding first is over (ADR 0004). What is not
# committed is the half-written copy an aborted run leaves beside it.
history/*.partial
dist/
.astro/
.env
.env.local
*.log
# Local project-link and CLI version state generated by Supabase.
supabase/.temp/
.agents/
.claude/
skills-lock.json
# Snapshots and console logs the browser tooling writes while a page is driven.
.playwright-mcp/
# What the browser vitest project writes when a component test fails. It is an
# artefact of one run, not evidence: the test that produced it is the record.
__screenshots__/